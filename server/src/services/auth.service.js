/**
 * Owner: Person 1 (Yong Wee) - Auth & User Management.
 *
 * Suggested responsibilities for this file:
 *  - hashPassword / verifyPassword (bcryptjs)
 *  - issueToken / verifyToken (jsonwebtoken, see middleware/auth.middleware.js)
 *  - CRUD against `users`, `saved_criteria_set`, `saved_criteria_item` tables
 *
 * Kept as a stub so Person 1 can build this out without touching routes/
 * or controllers/ owned by other workstreams.
 */

export {};
