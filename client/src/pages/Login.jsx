/**
 * Owner: Person 1 (Yong Wee) - Auth & User Management.
 * TODO: login/signup form, calling POST /api/auth/login and /api/auth/signup
 * (see client/src/api/client.js and server/src/routes/auth.routes.js).
 * On success, store the returned token (see api/client.js localStorage usage).
 */
export default function Login() {
  return (
    <section>
      <h1>Login / Signup</h1>
      <p>Owner: Person 1. Wire this up to /api/auth/login and /api/auth/signup.</p>
    </section>
  );
}
