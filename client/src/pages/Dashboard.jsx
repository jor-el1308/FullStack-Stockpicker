/**
 * Owner: Person 4 (Enrico) - Dashboard & Stock Report Page.
 * TODO: overview dashboard, likely linking into /stock/:exchangeCode/:stockCode
 * (see StockDetail.jsx) for the per-stock report page.
 */
export default function Dashboard() {
  return (
    <section>
      <h1>Dashboard</h1>
      <p>Owner: Person 4. Build the overview / saved screens here.</p>
    </section>
  );
}
