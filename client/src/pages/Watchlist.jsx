/**
 * Owner: Person 5 (Jayden) - Notifications & Optional AI Step.
 * TODO: list/add/remove watchlist items (GET/POST/DELETE /api/notifications/watchlist),
 * plus (optional) trigger the AI recommendation step via POST /api/notifications/ai-recommendation.
 */
export default function Watchlist() {
  return (
    <section>
      <h1>Watchlist</h1>
      <p>Owner: Person 5. Build watchlist + notification channel setup here.</p>
    </section>
  );
}
